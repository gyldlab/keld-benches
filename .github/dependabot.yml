# Keep the two full-SHA GitHub Action pins current without creating dependency
# churn for benchmark fixtures or harnesses.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
