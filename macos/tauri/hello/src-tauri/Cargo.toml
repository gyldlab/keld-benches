[package]
name = "tauri-hello"
version = "0.1.0"
description = "keld-benches macOS hello-window fixture (Tauri 2)"
authors = ["gyldlab"]
edition = "2021"

[lib]
name = "tauri_hello_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
